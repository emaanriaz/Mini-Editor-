/* Emaan Riaz w1669220
CISP 400 
Mini Project: Text Editor
*/
#include "editor.h"
#include <iostream>

int main() {
    
    Editor e;
    e.menu();
    // menu() function displays welcome message and complete textfile. Other methods (insert, delete, search, search&replace, and wordCount) are all called from menu(). 
    

    return 0;
}

